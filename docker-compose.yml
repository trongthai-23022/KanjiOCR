services:
  api:
    build:
      context: .
      dockerfile: Dockerfile.api
    image: KanjiOCR-api
  client:
    build:
      context: .
      dockerfile: Dockerfile.client
    image: KanjiOCR-client
    ports:
      - "3000:80"